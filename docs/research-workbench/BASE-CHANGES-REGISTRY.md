# 基座改动登记表（Base Changes Registry）

> 目的：科研工作台凡触及基座（upstream EKKOLearnAI/hermes-studio 同源）代码的改动，须在此登记，
> 保持 upstream 可同步能力。审查员按本表核验；未登记的基座侧改动视为违规。
> 口径：`packages/server/src/modules/research/**` 与 `tests/server/research-*` 属隔离域，不在本表范围。

| 编号 | 分支（工单） | 基座侧文件 | 改动性质 | upstream 同步策略 | 状态 |
| --- | --- | --- | --- | --- | --- |
| BC-1 | feature/p2-T1a-client-guard（W4） | `packages/client/src/views/hermes/WorkflowView.vue`（normalizeStoredNode/serializeWorkflowNodes/节点模板注册/运行时数据守卫）；`packages/client/src/components/hermes/workflow/`（新增只读确定性节点卡片组件 + types 扩展）；`packages/client/src/utils/workflow-node-type.ts`（新增纯函数层）；11 个 i18n locale | 画布节点 type 透传 + 只读防护：载入透传 `record.type`（缺省 `agent`，非 agent 类型不注入 agent 默认值）；序列化按 type 回写、不写 agent 字段；新增 script/validate/render 只读卡片；未知类型 vue-flow 默认兜底且往返无损；agent 分支行为零变化 | 纯增益、向后兼容，可原样提 PR upstream | ✅ 已合入 main（b902ae0 → merge 4f80927，pm 验收通过） |
| BC-2 | feature/p2-T1b-output-column（W5） | `packages/server/src/modules/studio/infrastructure/database/schemas.ts`（workflow_run_node_sessions 加 `output_json` 列 + 迁移）；`packages/server/src/modules/studio/repositories/workflow-run-store.ts`（record 字段 + create 缺省 + update 可选 outputJson 入参） | run-store 纯加法存储列：确定性节点输出落库（`TEXT NOT NULL DEFAULT ''`），迁移走 syncTable→addMissingSafeColumns 通用先例；update 函数签名向后兼容，现有调用方零修改；不触碰 manager.ts | 纯加法、向后兼容，可原样提 PR upstream | ✅ 已合入 main（4cadcdd → merge a196c85，pm 验收通过） |
| BC-3 | feature/p2-T1c-engine-dispatch（W6） | `packages/server/src/modules/studio/services/workflow/manager.ts`（normalize 类型分支；两调度器经唯一私有助手 executeDeterministicWorkflowNode 分发；preflight 跳过非 agent；rerun 恢复空 session 行改读 output_json）；`packages/server/src/modules/studio/services/workflow/deterministic-executor.ts`（新增：集中类型注册表 + script 子进程执行器）；`packages/server/src/modules/studio/public/workflow-runtime.ts`（可选 runDeterministicNode 依赖）；`packages/server/src/bootstrap/workflow-runtime-adapter.ts`（装配一行） | 引擎集中扩展：normalize 放行 script/validate/render（agent 分支逐字保留，未知类型显式拒绝且保留既有测试钉死的错误子串）；非 agent 节点跳过聊天会话创建，经 DI 执行器运行，输出落 output_json（session_id=''）；执行器按类型注册表集中分发，script= node 子进程（结构化 argv、无 shell 拼接、stdin 注入、SIGKILL 超时），validate/render 明确报错未配置；一处扩展 run/rerun 两路放行 | 附加式扩展（类型分支 + 可选 DI 依赖），agent 路径零变化，可原样提 PR upstream | ✅ 已合入 main（0a59987 → merge 55fdba1，pm 验收通过：148/148 实跑 + build 绿） |
| BC-4 | feature/p2-T3-canvas（W7） | `packages/client/src/components/hermes/workflow/WorkflowDeterministicNode.vue`（只读卡片升级为可配置卡片：script 的 code/runtime 字段编辑、title 编辑）；`packages/client/src/views/hermes/WorkflowView.vue`（节点托盘/拖拽创建 script/validate/render、新增节点 data 工厂与事务接线）；`packages/client/src/utils/workflow-canvas.ts`（确定性节点连接事务，若需）；11 个 i18n locale | 画布科研节点拖拽与配置（T2.3 验收）：三类型可拖入画布、可编辑配置（script: title/code，validate/render: title）、保存/载入保真（W4 序列化层已按 type 透传，新增字段自然流过）、未知类型兜底与 agent 行为零变化；不触 server | 纯增益、向后兼容，可原样提 PR upstream | ✅ 已合入 main（d0fc4e5 → merge 8a323ce，pm 验收通过：46/46 实跑 + build 绿） |
| BC-5 | feature/p2-fix-engine-lifecycle（codex P2 评审 F1 修复） | manager.ts（stopRun 取消集合纳入确定性行 + run 级 AbortSignal 传播 + deadline 取消底层操作 + rerun 复用 acquireRunAdmission + 终态重置原子 CAS + execution scope 唯一 token）；deterministic-executor.ts（超时/超限终止改走 killOwnedProcessTree；stdout/stderr 字节上限，超限终止并结构化报错；abort/close/timeout 单次结算）；workflow-runtime.ts（确定性请求增加取消句柄）；process-tree.ts（只读复用） | 引擎生命周期加固：停止可及时中止确定性脚本、Windows 进程树无孤儿、输出有上限、rerun 有 admission 与 CAS；agent 路径与既有测试钉死子串零变化 | 附加式加固（可选 DI 依赖扩展 + 既有 helper 复用），可原样提 PR upstream | ✅ 已合入 main（4626773 + 8a67795 测试超时修复 → merge f9de080，审查通过、pm 验收：deterministic 13/13 + manager 128/128 + tests/server 零回归） |
| BC-6 | feature/p2-fix-client-contracts（codex P2 评审 F2 修复） | workflow-node-type.ts（序列化/归一化三路径：agent / 已知确定性 / 未知类型原样保留 data 仅剥运行时 UI 字段；script 五字段契约守卫含 runtime）；WorkflowView.vue（按类型分流）；components/hermes/workflow/types.ts（增 runtime 字段）；api/studio/workflows.ts（增 output_json 同步）；11 个 i18n locale（若新增文案） | 客户端契约修复：未知类型往返真正无损（含 agent/model 等冲突 key）、output_json 类型同步、script runtime 守卫、卡片错误展示改进 | 纯增益、向后兼容，可原样提 PR upstream | ✅ 已合入 main（afa98f0 → merge 73c210a，审查 approve：客户端 1498 用例 + vue-tsc + build 绿） |
| BC-7 | feature/p3-T1-pdf + feature/p3-T3-latex（审查C P2 补登记） | `packages/client/src/api/studio/research-library.ts`、`packages/client/src/utils/research-paper-format.ts`（T3.1 新增）；`packages/client/src/api/studio/latex.ts`（T3.3 新增） | 客户端 base 目录下的 research 域 API helper 与纯工具函数，纯新增文件、零既有行改动 | 纯增益、upstream 零冲突 | ✅ 已合入 main（随 T3.1/T3.3 合入；2026-09-02 审查C 发现后 pm 补登记） |
| BC-8 | feature/p4-T2-chat-rag（W18，T4.2 聊天 @知识库 集成） | 11 个 i18n locale（`packages/client/src/i18n/locales/{en,zh,zh-TW,ar,de,es,fr,ja,ko,pt,ru}.ts`：research.rag 块内新增 chatSelectedHint/chatClearSelection/chatCitationsTitle/chatAskFailed/chatAskTimeout 五键）；聊天客户端文件（豁免范围、一并列出便于审查）：`ChatInput.vue`（@ 提及弹层 + 选中态标签 + 发送分流）、`MessageItem.vue`（引用溯源列表 + 错误提示 + 跳转）、`stores/hermes/chat.ts`（sendKnowledgeBaseMessage 编排 + Message 扩展字段）、新增 `stores/research/chat-knowledge.ts` | 聊天知识库问答：@ 提及选择已装载知识库；选中会话的纯文本消息经既有 /api/studio/research/rag/collections/:id/ask 客户端编排问答并回贴助手侧消息；答案展示引用列表（paperId→标题+页码+片段）并跳转 research.papers.preview；缺 key/sidecar 未配置以聊天内系统提示呈现；知识库未选中时普通聊天零变化。研究域服务端零改动 | 纯增益、向后兼容（locale 仅新增键、聊天组件附加式扩展），可原样提 PR upstream | ✅ 已合入 main（d936c31 → merge 49b343b，审查 approve：聊天管线逐 hunk 零回归，客户端 1591 用例 + vue-tsc + build 绿） |
| BC-9 | feature/p5-T1-e2e（W19，T5.1 运行完成通知） | `packages/client/src/views/hermes/WorkflowView.vue`（loadWorkflowRuns 中检测 run 状态迁出 queued/running → showSystemNotification，tag=workflow-run-complete-{runId}，clickUrl 深链运行详情；状态 map 只记最近一次）；11 个 i18n locale（workflow.runs 块内新增 notificationTitle/notificationCompleted/notificationFailed/notificationCanceled 四键） | 工作流运行完成通知：run 进入终态时经既有通知设施弹系统通知（桌面桥/浏览器 Notification/跨标签去重），前台不再抑制（requireBackground:false）；agent 与既有运行列表行为零变化 | 纯增益、向后兼容，可原样提 PR upstream | ✅ 已合入 main（3653207 → merge c3fa640，2026-09-02 e2e 全量 139 绿 + 客户端 1591 绿，pm 验收通过） |
| BC-10 | feature/p6-polish-t3（W22，P6 打磨：研究资源优雅关停） | `packages/server/src/bootstrap/http.ts`（additionalShutdownSteps 注册 'Research translation queue and databases' 步骤：stopTranslationQueueWorker→closeTranslationQueueDb→closePapersDb/closeLatexDb，懒加载） | 服务端优雅关停接入基座钩子：研究域翻译 worker 子进程树与三个 SQLite 在关停时有序关闭，纯新增注册步骤、无既有路径改动 | 纯增量、可原样提 PR upstream | ✅ 已合入 main（ec578c4 → merge 2210059，2026-09-02 pm 验收通过） |
| BC-11 | feature/p7-kb-history（P7 工单 1+2：聊天知识库问答落服务端历史 + 选择态持久化） | 客户端聊天基座文件：`packages/client/src/stores/hermes/chat.ts`（sendKnowledgeBaseMessage 改调 POST /api/studio/research/rag/chat-asks 服务端编排；乐观消息采纳服务端消息 id 去重/替换；新增 hydrateRagChatAsks 在 5 处历史加载点水合引用；Message 字段零变化）；`packages/client/src/components/hermes/chat/ChatInput.vue`（恢复选择懒校验 watch、发送分支持久化 id 兜底、失效提示条 + 样式）；`packages/client/src/api/studio/research-knowledge.ts`（新增 chatAsk/getChatAsk/listSessionChatAsks + RagChatAsk 类型）；11 个 i18n locale（research.rag 块新增 chatSelectionExpired 一键）；`tests/client/chat-knowledge-locales.test.ts`（键清单扩展） | 聊天知识库问答从纯客户端编排升级为服务端编排落历史：服务端经既有 studio/public/sessions 门面写入会话历史（基座服务端零改动、消息表结构零变化），引用经研究域自有 rag_chat_asks 绑定表按消息 id 水合；选择态 localStorage 持久化 + 懒校验失效清理提示。基座侧均为附加式改动，普通聊天与 agent 运行路径零变化 | 纯增益、向后兼容（locale 仅新增键、聊天组件附加式扩展），可原样提 PR upstream | ✅ 已合入 main（46a190c → merge 23b954b，2026-09-03 pm 验收：服务端 research 13 文件 + 客户端全量 + build 绿） |

> 备注：本地 main（22b703e）已含 BC-1~11（2026-09-03，P2~P6 合入 + P7 用户可感知限制清零；BC-11 为聊天知识库问答落服务端历史）。origin 已推送至 6b583cd，P7 批推送待确认。
