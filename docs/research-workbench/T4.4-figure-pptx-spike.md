# T4.4 绘图工作流 pptx 导出 Spike 报告：python-pptx sidecar 真实链路

- 分支：`feature/p4-T4-skills`（基线 main 80944f5）；日期：2026-09-02
- 结论速览：**真实链路已打通**。`%TEMP%` venv（Python 3.14.3）+ `pip install python-pptx`（清华镜像）→ python-pptx 1.0.2；figure-drawing 工作流的确定性渲染节点产出 `figure.svg` 后，门控 pptx 节点经 python-pptx sidecar 把图形要素落成**原生可编辑形状**的 `.pptx`。**本仓库零新增 npm 依赖**，Python 环境全部在仓库外（%TEMP%）。
- 证据分级：**测试实证**（gated vitest 5/5，其中真实 pptx 用例经 python-pptx 回读校验）> 命令输出摘录 > 推断。

---

## 1. 环境可用性

| 项 | 结果 |
| --- | --- |
| venv 创建 | `C:\Python314\python.exe -m venv %TEMP%\figure-pptx-spike\venv` 正常（Python 3.14.3，不入库） |
| PyPI 网络 | 清华镜像 `pypi.tuna.tsinghua.edu.cn` 可用（与 T3.2 spike 结论一致） |
| python-pptx 安装 | `pip install -i https://pypi.tuna.tsinghua.edu.cn/simple python-pptx` → **1.0.2**，依赖 lxml 等，秒级装完，无版本坑 |
| 直接运行 | `python figure_svg_to_pptx.py in.svg out.pptx "标题"` → `{"ok": true, "shapes": 4, "slidePx": [900, 560]}`，产物 28 KB |

## 2. 设计：SVG 约束词表 → 原生形状映射

关键取舍：**不嵌入 SVG 位图**（python-pptx 原生不支持 SVG 图片，转 PNG 需要重量级渲染依赖），而是让绘图 agent 按 `scientific-figure-style` 技能钉死的词表出图（rect/circle/ellipse/line/polyline/polygon/text + g 平移），sidecar 把词表逐元素映射为**原生可编辑形状**：

| SVG 元素 | pptx 形状 | 说明 |
| --- | --- | --- |
| `rect`（rx>0） | ROUNDED_RECTANGLE | fill/stroke 颜色、线宽随迁 |
| `rect`（rx=0） | RECTANGLE | 同上 |
| `circle` / `ellipse` | OVAL | r/rx/ry → 外接框 |
| `line` | STRAIGHT connector | stroke/stroke-width → 线色/线宽 |
| `polyline` / `polygon` | 连续 STRAIGHT connector | 逐段连线（填充不保） |
| `text` | textbox | y 基线→框顶上移一个字号；text-anchor middle/end 按估算宽度回退；px→pt（×0.75）；font-weight bold 随迁 |
| `g transform="translate/matrix"` | 坐标平移累加 | 只取平移分量 |

画布：幻灯片尺寸直接设为 SVG 的 width/height（96 px/in → 9525 EMU/px），900×560 SVG → 900×560 px 幻灯片，坐标零缩放失真。标题写入 core_properties。z-order 按 SVG 文档顺序保持。

## 3. 门控机制（不可用即优雅降级）

pptx 导出是 figure-drawing 模板的**可选第 4 节点**（确定性 script 节点，无引擎改动）：

- `RESEARCH_FIGURE_PPTX_PYTHON` 未设置或 sidecar 路径（`RESEARCH_FIGURE_PPTX_SIDECAR`，必须绝对路径）缺失 → 节点输出 `{"pptxExported": false, "reason": ...}` **正常退出**，run 照常 completed，`figure.svg` 为主产物。
- 已设置 → spawn（argv 数组、shell:false）执行 sidecar；产物缺失/非零退出同样降级并带 stderr 尾部原因。
- 两个环境变量均已登记在模板 `optionalEnv`，API 层可见。

**教训记录**：脚本节点在 run 工作区目录内执行（引擎传 workspace 为 cwd），一切「按仓库布局猜相对路径」的默认值都不可靠——sidecar 路径因此必须用绝对路径环境变量，而不是 cwd 相对默认值（实现期发现并已修正）。

## 4. 自动化实证（本分支新增）

`tests/server/research-figure-drawing-e2e.test.ts` 共 5 用例（真实引擎 + 真实 `node -e` 子进程，仅 mock 聊天层）：

1. 未装载 skill 包时 runNow 被引擎 preflight 拒绝（`requires unavailable skill: scientific-figure-style`）；
2. 主链路 e2e：intake → agent（绑定 skill，内容被引擎注入 prompt）→ render 真落盘 `figure.svg`（柱高与数据严格成比例 4.75px/单位、无引擎包装行泄漏）→ pptx 门控降级 `pptxExported:false`；
3. 非法 brief（缺 outDir）确定性失败；
4. agent 输出无 SVG 时 render 节点确定性失败；
5. **gated 真实 pptx 用例**（`HERMES_FIGURE_PPTX_SMOKE=1` + `RESEARCH_FIGURE_PPTX_PYTHON` 指向含 python-pptx 的解释器）：真实产物 `figure.pptx`（>10 KB、PK 头），并用 python-pptx 回读断言 ≥10 个原生形状、幻灯片 900×560。

实跑结果：门控开启 **5/5 通过**（gated 用例 2.1s）；门控关闭 **4/4 通过 + 1 skip**（CI 无需 Python）。

## 5. 已知限制与生产部署清单

- SVG 子集约束：`tspan`、贝塞尔 `path`（仅支持简单折线段）、渐变、裁剪、旋转（除 g 平移）不映射；绘图技能已把词表钉在子集内，agent 越界时由 render/导出节点确定性报错，不静默失真。
- 文本宽度为估算值（len×0.62×字号），`text-anchor=end` 的长标签可能偏移数像素；不影响数据图元。
- px→pt 固定 ×0.75（96dpi 假设）。
- 部署清单：Python 3.10+ venv + `pip install python-pptx`（GFW 内网加 `-i https://pypi.tuna.tsinghua.edu.cn/simple`）；服务端环境变量设 `RESEARCH_FIGURE_PPTX_PYTHON=<venv python 绝对路径>`、`RESEARCH_FIGURE_PPTX_SIDECAR=<仓库>/packages/server/src/modules/research/workflows/scripts/figure_svg_to_pptx.py`。
- 人工复现：
  1. `python -m venv %TEMP%\figure-pptx-spike\venv && %TEMP%\figure-pptx-spike\venv\Scripts\pip install python-pptx`；
  2. `RESEARCH_FIGURE_PPTX_PYTHON=<venv python> HERMES_FIGURE_PPTX_SMOKE=1 npx vitest run tests/server/research-figure-drawing-e2e.test.ts` 应 5/5。
